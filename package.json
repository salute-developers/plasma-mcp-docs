{
  "name": "plasma-mcp-docs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --noEmit",
    "generate": "npx tsx scripts/generate-site.ts",
    "split-json": "npx tsx scripts/split-output-json.ts",
    "clean": "rm -rf dist",
    "serve": "npx http-serve dist -p 8080 -o",
    "preview": "npm run generate && npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/marked": "^5.0.0",
    "@types/node": "^20.0.0",
    "http-serve": "^1.0.1",
    "marked": "^9.0.0",
    "prettier": "^3.8.1",
    "tsx": "^4.20.6",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
